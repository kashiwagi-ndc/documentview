# 仕様メモ（工程③ 反復の計画づくり）— 認証（Azure AD SSO）

> **【次フェーズに延期】** 今回のミッション（表示機能の画面・動作イメージ固め）では対象外。既存システム連携を検討する段階になったら着手する参考資料として残す。

## 今回やること
- **機能名**：認証（Azure AD / Entra ID SSOログイン）
- **今回の目標（スプリントゴール）への寄与**：社内の認証済みユーザーだけが本アプリにアクセスできる状態を作る（文書単位の可否判定は基盤システムの責務であり本機能では行わない）

## 仕様（AIが迷わないように具体的に）
| 項目 | 内容 |
|---|---|
| 対象ファイル | `backend/src/main/java/jp/nihondata/documentview/security/SecurityConfig.java`、`frontend/src/auth/AuthProvider.tsx`、`frontend/src/auth/msalConfig.ts` |
| 対象の関数・IF | バックエンド：Spring Security の OAuth2 Login / Resource Server 設定（OIDC, Azure AD テナント）。フロントエンド：MSAL（`@azure/msal-react`）による `useIsAuthenticated()` / `loginRedirect()` |
| 対象外（やらないこと） | 文書・案件単位のアクセス可否判定（基盤システムの責務）、ロールベースアクセス制御、パスワード方式のログイン、多要素認証の独自実装（Azure AD側の設定に委譲） |
| 動作確認手順（できたの基準） | ①未ログインで `/` にアクセス→Azure ADログイン画面へリダイレクトされる／②正しい社内アカウントでログイン→アプリ画面が表示される／③未ログイン状態でバックエンドAPI（例：`/api/cases/{id}/tree`）を直接叩く→401が返る／④ログアウト操作でセッションが破棄され再度①の状態に戻る |

## 不確実性（まだ確定していないこと）
| 種類 | 内容 | いつ・どう確かめる |
|---|---|---|
| 複雑性 | Azure AD（Entra ID）テナント上でのアプリ登録（クライアントID・リダイレクトURI・テナントID）がまだの場合、実装がブロックされる | 着手前にテナント管理者へアプリ登録を依頼。登録情報を受領してから着手 |
| 曖昧性 | セッションの有効期限・トークンリフレッシュ方針が未定 | 情シスの既存SSO運用ポリシーに合わせる（要確認） |

## 完成条件メモ（⑤テスト・⑥レビューの合格ライン）
- [ ] 未ログイン時のリダイレクトとAPI 401が自動テストで確認できる
- [ ] クライアントシークレット等の機密情報がリポジトリにコミットされていない（環境変数・シークレットストア経由）
- [ ] HTTPS前提の設定になっている（本番相当環境で確認）
